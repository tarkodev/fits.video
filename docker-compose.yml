services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: fitsvideo-front
    container_name: fitsvideo-front
    ports:
      - "${WEB_PORT:?}:80"
    restart: unless-stopped
